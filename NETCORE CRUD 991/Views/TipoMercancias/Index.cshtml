@model IEnumerable<NETCORE_CRUD_991.Models.Entities.TipoMercancia>

@{
    ViewData["Title"] = "Index";
}

<h1>Tipos de Mercancia</h1>

<p>
    <a asp-action="CrearEditar" class="btn btn-secondary btn-block"><i class="fas fa-backspace"></i> Crear Tipo de Mercancia</a>

</p>

<form asp-action="Index" method="post">
    <div class="form-actions no-color">
        <p>
            Filtro: <input type="text" name="busqueda" value="" placeholder="Buscar..." />
            <input type="submit" value="Buscar" class="btn btn-primary" />
            <a asp-action="Index">Mostrar todos los registros</a>
        </p>
    </div>
</form>


<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoMercancia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdTipoMercancia)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    <a asp-action="CrearEditar" class="text-primary" asp-route-id="@item.IdTipoMercancia"><i class="fas fa-user-edit"></i> Editar</a> |
                    <a asp-action="Detalle" class="text-info" asp-route-id="@item.IdTipoMercancia"><i class="fas fa-search-plus"></i> Detalle</a> |
                    <a asp-action="Eliminar" onclick="return confirm ('¿Desea eliminar este tipo de mercancia?')" class="text-danger" asp-route-id="@item.IdTipoMercancia"><i class="fas fa-trash-alt"></i> Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
